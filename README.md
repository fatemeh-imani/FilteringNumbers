📌 مرحله هفتم: استفاده از Extension Method ها

در این مرحله هدف این است که متدهای پروژه را قابل استفاده‌تر، تمیزتر و شی‌ءگرا‌تر کنیم.
برای این کار، بخش‌هایی از NumberService که روی یک Collection کار می‌کردند، به صورت Extension Method نوشته شدند.


ویژگی مهم این روش:
متدهای جدید مثل متدهای خود کلاس صدا زده می‌شوند.

🎯 چرا از Extension Method استفاده کردیم؟

در پروژه ما، قبل از مرحله 7 متدهای زیر در NumberService به صورت static وجود داشتند:

FilterObjects(IEnumerable<T>, Func<T,bool>)

PrintItems(IEnumerable<T>)

اما:

هر بار باید این متدها را با صدا زدن کلاس فراخوانی می‌کردیم
مثل:

NumberService.FilterObjects(numbers, condition);


کد طولانی می‌شد

خوانایی کم می‌شد

روح شی‌ءگرایی (OOP) ضعیف می‌شد

با Extension Method این ساختار بسیار خواناتر شد:

var filtered = numbers.FilterObjects(x => x > 10);
filtered.PrintItems();


این یعنی:

مجموعه‌ی ما (IEnumerable<T>) حالا خودش توانایی فیلتر کردن و چاپ کردن را دارد.

NumberService همچنان static باقی مانده و معماری پروژه حفظ شده.



✅ مزایای استفاده از Extension Method
✔ 1. خوانایی بهتر و کد تمیزتر

کد بسیار شبیه LINQ می‌شود و فهم آن ساده‌تر است.

✔ 2. اضافه کردن رفتار جدید بدون تغییر کلاس اصلی

بدون دست زدن به NumberService، قابلیت‌های جدید اضافه شد.

✔ 3. مناسب برای کدهای زنجیره‌ای (Fluent Style)
numbers.FilterObjects(x => x > 10)
       .FilterObjects(x => x < 50)
       .PrintItems();

✔ 4. تطبیق آسان با هر نوع Collection

List، Array، IEnumerable → همه پشتیبانی می‌شوند.

⚠️ معایب Extension Method
❌ 1. بیش از حد استفاده شود، کد را گیج‌کننده می‌کند

اگر برای هر چیز کوچک extension بنویسیم، پروژه سنگین و گیج‌کننده می‌شود.

❌ 2. اولویت با متدهای کلاس اصلی است

اگر همین اسم در کلاس اصلی وجود داشته باشد، ممکن است اشتباه انتخاب شود.

❌ 3. Debug کردن کمی سخت‌تر می‌شود

چون متدها در ظاهر بخشی از کلاس اصلی دیده می‌شوند.